import { AppFontSizeDirective } from './app-font-size.directive';

describe('AppFontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AppFontSizeDirective();
    expect(directive).toBeTruthy();
  });
});
