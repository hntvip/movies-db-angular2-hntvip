import { AppHightLightDirective } from './app-hight-light.directive';

describe('AppHightLightDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHightLightDirective();
    expect(directive).toBeTruthy();
  });
});
